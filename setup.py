from setuptools import find_packages, setup

setup(
    name = "pynotesutils",
    packages = find_packages(["pynotes"]),
    version = "0.1.0",
    description = "A library exec and view output scripts in notes on android",
    author = "Daniel",
    install_requires = []
)
